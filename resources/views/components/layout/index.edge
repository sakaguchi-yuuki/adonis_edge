<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ tile || "SMP"}}</title>
  @vite(['resources/js/app.js'])
  @stack('dumper')
  @if($slots.meta)
  {{{ await $slots.meta()}}}
  @end
</head>

<body>
  <div class="wrap">
    @include('partials/header')
    <div>
      @include('partials/side_bar')
      {{{ await $slots.main()}}}
    </div>
    @include('partials/footer')
  </div>
</body>

</html>
